library(tidyverse)
## TASK: Plot `price` vs `carat` below
## Your code here!
ggplot() +
geom_line(aes(x = carat, y = price, linewidth = n))
## TASK: Plot `price` vs `carat` below
## Your code here!
diamonds %>%
ggplot() +
geom_line(aes(x = carat, y = price, linewidth = n))
## TODO: Complete this code
diamonds %>%
# This is the focus of this code: Note the
# additional `linewidth` argument to aes()
ggplot() +
geom_point(aes(x = carat, y = price, colour = cut ))
## TODO: Complete this code
diamonds %>%
ggplot() +
geom_line(aes(x = carat, y = price, colour = cut ))
## TODO: Complete this code
diamonds %>%
ggplot() +
geom_line(aes(x = carat, y = price ))
## TODO: Complete this code
diamonds %>%
ggplot() +
geom_point(aes(x = carat, y = price ))
## TODO: Complete this code
diamonds %>%
# This is the focus of this code: Note the
# additional `linewidth` argument to aes()
ggplot() +
geom_point(aes(x = carat, y = price, colour = cut ))
## TODO: Complete this code
diamonds %>%
# This is the focus of this code: Note the
# additional `linewidth` argument to aes()
ggplot() +
geom_point(aes(x = cut, y = price, colour = carat ))
## TODO: Complete this code
diamonds %>%
# This is the focus of this code: Note the
# additional `linewidth` argument to aes()
ggplot() +
geom_point(aes(x = cut, y = carat, colour = price ))
## TODO: Complete this code
diamonds %>%
# This is the focus of this code: Note the
# additional `linewidth` argument to aes()
ggplot() +
geom_point(aes(x = price, y = carat, colour = cut ))
## TODO: Complete this code
diamonds %>%
# This is the focus of this code: Note the
# additional `linewidth` argument to aes()
ggplot() +
geom_point(aes(x = carat, y = price, colour = cut ))
